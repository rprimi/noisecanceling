% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_item_psicom_si.R
\name{save_item_psicom_si}
\alias{save_item_psicom_si}
\title{Export single-set psychometric results to an Excel workbook}
\usage{
save_item_psicom_si(obj, filename)
}
\arguments{
\item{obj}{A list with \code{psicom}, \code{alpha_item_stat}, \code{alpha_scale_stat} and
\code{item_dic} elements, as returned by \code{\link[=score_tests]{score_tests()}}.}

\item{filename}{Path of the \code{.xlsx} file to create.}
}
\value{
Invisibly returns \code{filename}. Called for the side effect of writing
a workbook with the sheets \code{item_stats}, \code{alpha_item_stat}, \code{scale_stat}
and \code{scale_cor}.
}
\description{
A simplified counterpart of \code{\link[=save_item_psicom]{save_item_psicom()}} for results that have a
single set of scores (no original/recoded split), such as the output of
\code{\link[=score_tests]{score_tests()}}. Writes item statistics, item-level alpha statistics,
scale statistics and the scale correlation matrix.
}
\examples{
\dontrun{
data(data_senna)
data(senna_dic)
res <- score_tests(data_senna, senna_dic)
save_item_psicom_si(res, filename = tempfile(fileext = ".xlsx"))
}
}
\seealso{
\code{\link[=score_tests]{score_tests()}}, \code{\link[=save_item_psicom]{save_item_psicom()}}
}
