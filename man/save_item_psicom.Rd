% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_item_psicom.R
\name{save_item_psicom}
\alias{save_item_psicom}
\title{Export psychometric results to an Excel workbook}
\usage{
save_item_psicom(obj, filename)
}
\arguments{
\item{obj}{A list as returned by \code{\link[=find_psychometrics]{find_psychometrics()}}.}

\item{filename}{Path of the \code{.xlsx} file to create.}
}
\value{
Invisibly returns \code{filename}. Called for the side effect of writing
the workbook, which has the sheets \code{item_stats}, \code{scale_stats_ori},
\code{scale_stats_rec}, \code{scale_cor_o}, \code{scale_cor_r}, \code{alpha_ori} and
\code{alpha_rec}.
}
\description{
Writes the results of \code{\link[=find_psychometrics]{find_psychometrics()}} to a multi-sheet Excel file,
combining item-level statistics, scale-level statistics and scale
correlations for both the original and the acquiescence-recoded scores.
}
\examples{
\dontrun{
data(data_senna)
data(senna_dic)
recoded <- recode_for_acq(data_senna, senna_dic)
psicom <- find_psychometrics(recoded, likert = 5, center = 3)
save_item_psicom(psicom, filename = tempfile(fileext = ".xlsx"))
}
}
\seealso{
\code{\link[=find_psychometrics]{find_psychometrics()}}, \code{\link[=save_item_psicom_si]{save_item_psicom_si()}}
}
