% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/item_histograms.R
\name{item_histograms}
\alias{item_histograms}
\title{Plot item response distributions by scale and pole}
\usage{
item_histograms(df, coditems, scales, poles, r_levels)
}
\arguments{
\item{df}{A data frame of item responses.}

\item{coditems}{Character vector of item column names in \code{df}.}

\item{scales}{Character vector (same length as \code{coditems}) giving the scale
of each item.}

\item{poles}{Vector (same length as \code{coditems}) giving the pole of each item
(\code{1} positively keyed, \code{0} negatively keyed).}

\item{r_levels}{Vector of the ordered response categories (e.g. \code{1:5}).}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Builds a faceted bar chart of the proportion of respondents choosing each
response category, with one facet per scale and per item pole. Useful for
inspecting whether positively and negatively keyed items behave symmetrically
before correcting for acquiescence.
}
\examples{
data(data_senna)
data(senna_dic)
d <- senna_dic[senna_dic$scale == "C", ]
item_histograms(data_senna, d$coditem, d$scale, d$pole, r_levels = 1:5)
}
