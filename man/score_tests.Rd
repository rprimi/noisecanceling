% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_tests.R
\name{score_tests}
\alias{score_tests}
\title{Score tests and compute classical psychometrics}
\usage{
score_tests(
  df,
  item_dic,
  scr_tot = FALSE,
  filename = "item_stats.xlsx",
  save_item_stat = FALSE,
  reversed = FALSE,
  score_fast = FALSE,
  min_values = NULL,
  max_values = NULL
)
}
\arguments{
\item{df}{A data frame of item responses.}

\item{item_dic}{Item dictionary with at least \code{coditem}, \code{scale} and \code{pole}
columns. See \code{\link[=recode_for_acq]{recode_for_acq()}} for the expected format.}

\item{scr_tot}{Logical. Passed to \code{totals}: if \code{TRUE}, total (summed) scores
are returned instead of means. Defaults to \code{FALSE}.}

\item{filename}{Path of the Excel file written when \code{save_item_stat = TRUE}.}

\item{save_item_stat}{Logical. If \code{TRUE}, item and scale statistics are
written to \code{filename}. Defaults to \code{FALSE}.}

\item{reversed}{Logical. Passed to \code{\link[=dic2keys]{dic2keys()}}: whether negatively keyed
items have already been reverse-scored. Defaults to \code{FALSE}.}

\item{score_fast}{Logical. If \code{TRUE}, use the faster \code{\link[psych:score.items]{psych::scoreFast()}}
instead of \code{\link[psych:score.items]{psych::scoreItems()}}. Defaults to \code{FALSE}.}

\item{min_values, max_values}{Optional numeric vectors of the minimum and
maximum possible value of each scored item. If \code{NULL} (the default) they
are taken from the observed range of the scored items.}
}
\value{
A list with the scoring object (\code{psicom}), the \code{keys} matrix, the
nested \code{alpha} results, and tidy \code{alpha_scale_stat} / \code{alpha_item_stat}
data frames, plus the \code{item_dic}.
}
\description{
Scores one or more scales from an item dictionary using
\code{\link[psych:score.items]{psych::scoreItems()}} (or \code{\link[psych:score.items]{psych::scoreFast()}}) and computes per-scale
reliability statistics. Unlike \code{\link[=find_psychometrics]{find_psychometrics()}}, this function works
on a single data frame and does not require an \code{\link[=recode_for_acq]{recode_for_acq()}} object,
so it can be used to score either raw or acquiescence-recoded data.
}
\examples{
data(data_senna)
data(senna_dic)
res <- score_tests(data_senna, senna_dic)
res$alpha_scale_stat
}
\seealso{
\code{\link[=find_psychometrics]{find_psychometrics()}}, \code{\link[=dic2keys]{dic2keys()}}
}
