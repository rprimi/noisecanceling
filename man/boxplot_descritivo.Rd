% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boxplot_descritivo.R
\name{boxplot_descritivo}
\alias{boxplot_descritivo}
\title{Faceted boxplots of scale scores with standardized regressions}
\usage{
boxplot_descritivo(df, scales, x_var, grid_x_var, breaks, intercept)
}
\arguments{
\item{df}{A data frame containing the scale scores and the grouping
variables.}

\item{scales}{Character vector of scale-score column names in \code{df}.}

\item{x_var}{Name of the variable on the x axis (and a model predictor).}

\item{grid_x_var}{Name of the variable used for row facetting (and the
second model predictor).}

\item{breaks}{Numeric vector of y-axis breaks.}

\item{intercept}{Numeric. Where to draw a horizontal reference line.}
}
\value{
A list with \code{plot} (a \pkg{ggplot2} object) and \code{regressions} (a
data frame of standardized betas and R-squared, one row per scale).
}
\description{
Draws faceted boxplots of one or more scale scores against a grouping
variable and, for each scale, fits a linear model predicting the scale score
from a predictor and a grouping variable, returning the standardized
regression coefficients and R-squared.
}
