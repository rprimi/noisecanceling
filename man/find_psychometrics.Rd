% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_psychometrics.R
\name{find_psychometrics}
\alias{find_psychometrics}
\title{Compute classical psychometrics for original and acquiescence-recoded scores}
\usage{
find_psychometrics(obj, likert = 5, center = 0)
}
\arguments{
\item{obj}{A list as returned by \code{\link[=recode_for_acq]{recode_for_acq()}}.}

\item{likert}{Integer. The number of response categories of the Likert
scale, used to reverse-score negatively keyed items in the original data
(\code{likert + 1 - x}). Defaults to \code{5}.}

\item{center}{Numeric. Value added back to the centered responses after
reverse-scoring, so recoded scores sit on an interpretable scale. Defaults
to \code{0}.}
}
\value{
A list with:
\describe{
\item{scores}{Data frame of scale scores (original \verb{_ori} and recoded
\verb{_rec}) together with the acquiescence indices.}
\item{psicom_orig, psicom_recoded}{The \code{\link[psych:score.items]{psych::scoreItems()}} objects for
the original and recoded data.}
\item{alpha_orig_scale_stat, alpha_rec_scale_stat}{Per-scale reliability
statistics.}
\item{alpha_orig_item_stat, alpha_rec_item_stat}{Per-item statistics
(including \code{r.drop}).}
\item{item_dic}{The item dictionary.}
\item{keys}{The scoring keys matrix.}
}
}
\description{
Takes the output of \code{\link[=recode_for_acq]{recode_for_acq()}}, reverse-scores negatively keyed
items, and runs a classical psychometric analysis (scale scores, Cronbach's
alpha, item-total correlations) on both the original responses and the
acquiescence-recoded responses. Comparing the two shows how much
acquiescence inflated or distorted the original results.
}
\examples{
data(data_senna)
data(senna_dic)
recoded <- recode_for_acq(data_senna, senna_dic)
psicom <- find_psychometrics(recoded, likert = 5, center = 3)
psicom$alpha_orig_scale_stat
}
\seealso{
\code{\link[=recode_for_acq]{recode_for_acq()}}, \code{\link[=save_item_psicom]{save_item_psicom()}}
}
