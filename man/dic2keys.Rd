% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dic2keys.R
\name{dic2keys}
\alias{dic2keys}
\title{Build a scoring keys matrix from an item dictionary}
\usage{
dic2keys(item_dic, reversed = TRUE)
}
\arguments{
\item{item_dic}{A data frame with at least the columns \code{scale}, \code{pole} and
\code{coditem}. An \code{order} column is added automatically if absent.}

\item{reversed}{Logical. If \code{TRUE} (the default), negatively keyed items are
assumed to have already been reverse-scored, so all items keep a positive
sign in the keys. If \code{FALSE}, negatively keyed items (\code{pole == 0}) are
given a negative sign so \code{\link[psych:score.items]{psych::scoreItems()}} reverses them.}
}
\value{
A numeric matrix with one row per item and one column per scale.
}
\description{
Converts an item dictionary into a scoring keys matrix in the format
expected by \code{\link[psych:score.items]{psych::scoreItems()}}. Each column is a scale; each cell is
\code{+1}, \code{-1} or \code{0} indicating how the item loads on that scale.
}
\keyword{internal}
